---
icon: qrcode
---

# Apriltag Pipeline

## What Are AprilTags?

AprilTags are visual fiducial markers designed for accurate detection and position estimation using cameras.

An AprilTag is a square marker containing a unique black and white pattern. Each tag has an ID that allows a camera to identify which specific tag it is looking at. Unlike normal QR codes, AprilTags are optimized for robotics applications where precise position and orientation measurements are required.

AprilTags are commonly used in robotics for:

* Robot localization
* Autonomous navigation
* Object tracking
* Camera calibration
* Target alignment

In FRC, AprilTags are used as fixed reference points around the field. The robot uses a camera to detect these tags and calculate its own position relative to the field.
