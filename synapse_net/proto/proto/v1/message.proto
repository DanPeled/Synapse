syntax = "proto3";

package proto.v1;

import "google/protobuf/any.proto";

message MessageProto { // Generic message for the messages sent via the socket
  string type = 1;
  google.protobuf.Any payload = 2;
}
